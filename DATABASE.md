# DATABASE - SOS Web

Database SQL previsto: MariaDB/MySQL.

ORM iniziale: Prisma.

Lo schema iniziale vive in:

    backend/prisma/schema.prisma

I documenti legacy GAS sono in:

    docs/legacy-gas/DATABASE_GAS.md

Prima di modificare lo schema, aggiornare questo file e poi la migration Prisma.
