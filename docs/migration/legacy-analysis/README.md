# Migrazione Futura

Questa cartella contiene analisi e ipotesi per una possibile migrazione futura da Google Apps Script + Google Sheets a web app con database SQL.

Non e' la roadmap corrente. La versione operativa resta Apps Script, con schema documentato in `DATABASE.md` e deploy documentato in `DEPLOYMENT.md`.
